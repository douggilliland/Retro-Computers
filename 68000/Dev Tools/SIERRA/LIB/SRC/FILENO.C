#include <stdio.h>

/*---------------------------------- fileno() -------------------------------*/

/*
 * fileno returns the integer file descriptor associated with the stream
 * pointed to by fp
 */

int (fileno)(register FILE *fp)
{
    return(fileno(fp));
}
