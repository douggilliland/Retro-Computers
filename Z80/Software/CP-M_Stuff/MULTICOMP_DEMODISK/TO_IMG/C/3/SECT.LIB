Package Sect Is
    -- Used to define an example Type for use with the standard JANUS/Ada
    -- Package Randio.

    Type Sector Is Array (0..127) Of Byte;

End Sect;
  End End_of_File;
	
End RandIO;
 Return Count(Fmask.FCB.Random_rec1-1) * REC_PER_BLOCK;
	Else
	    Return Count((Fmask.FCB.Random_rec1-1) / BLOCK_PER_REC);
ile, in records.
    Begin
	If Fyle = Null Then
	    Put("** File Not Open"); Util.Err_Exit;
	End If;
	fmask := Util.FConve