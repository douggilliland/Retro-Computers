Package srandom Is

    --
    -- Simple random number generator
    --

    Procedure Set_Seed;
	-- Initialize the random number generator to its default seeds

    Procedure Set_Seed(x,y,z : Integer);
	-- Sets the seeds to any three arbitrary integers

    Function rand return float;
	-- Returns a random number in the range 0..1

End srandom;

