A minimal input/output library for a tiny subset of stdio functions.
Very minimal for now in fact!

This requires the underlying target provides

__getc 		- read keyboard ascii code into B, blocking, preserve X
__putc		- write char B to output, must preserve X

